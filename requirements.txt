# Known-good versions validated on the author's machine (2026-06-20, Python 3.13).
# This is the reproducible LOCK; for a relaxed install use `pip install -e .`
# (pyproject.toml carries conservative lower bounds instead). Updating mascope-sdk
# should be paired with a green test run — the match-tree parser tracks its
# response shapes.
mascope-sdk==2026.5.12
pandas==3.0.3
numpy==2.3.0
scipy==1.17.1
matplotlib==3.10.3
openpyxl==3.1.5
python-dotenv==1.1.0
